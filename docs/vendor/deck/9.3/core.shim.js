// ESM shim for @deck.gl/core — re-exports named bindings from the accumulated
// globalThis.deck namespace populated by the vendored self-contained UMD bundles
// (deck-core/layers/extensions/mapbox .umd.js), which the HTML loads as classic
// <script> tags (in that order) BEFORE this module evaluates. Zero CDN, no bundler,
// no React: this is a same-origin re-export, not a network fetch.
const D = globalThis.deck;
if (!D || !D.Deck) throw new Error('@deck.gl/core shim: globalThis.deck not populated — load deck-*.umd.js (core first) before the import-map module');
if (!D.Deck) throw new Error('@deck.gl/core shim: expected globalThis.deck.Deck — bundle load order wrong');
const {
  AmbientLight,
  Attribute,
  AttributeManager,
  COORDINATE_SYSTEM,
  CompositeLayer,
  Controller,
  Deck,
  DeckGL,
  DeckRenderer,
  DirectionalLight,
  FirstPersonController,
  FirstPersonView,
  FirstPersonViewport,
  FlyToInterpolator,
  Layer,
  LayerExtension,
  LayerManager,
  LightingEffect,
  LinearInterpolator,
  MapController,
  MapView,
  OPERATION,
  OrbitController,
  OrbitView,
  OrbitViewport,
  OrthographicController,
  OrthographicView,
  OrthographicViewport,
  PointLight,
  PostProcessEffect,
  TRANSITION_EVENTS,
  TerrainController,
  Tesselator,
  TransitionInterpolator,
  UNIT,
  VERSION,
  View,
  Viewport,
  WebMercatorViewport,
  Widget,
  _CameraLight,
  _Component,
  _ComponentState,
  _GlobeController,
  _GlobeView,
  _GlobeViewport,
  _LayersPass,
  _PickLayersPass,
  _SunLight,
  _applyStyles,
  _compareProps,
  _count,
  _deepEqual,
  _fillArray,
  _flatten,
  _memoize,
  _mergeShaders,
  _registerLoggers,
  _removeStyles,
  assert,
  color,
  createIterable,
  fp64LowPart,
  getShaderAssembler,
  gouraudMaterial,
  log,
  phongMaterial,
  picking,
  project,
  project32,
  shadow
} = D;
export {
  AmbientLight,
  Attribute,
  AttributeManager,
  COORDINATE_SYSTEM,
  CompositeLayer,
  Controller,
  Deck,
  DeckGL,
  DeckRenderer,
  DirectionalLight,
  FirstPersonController,
  FirstPersonView,
  FirstPersonViewport,
  FlyToInterpolator,
  Layer,
  LayerExtension,
  LayerManager,
  LightingEffect,
  LinearInterpolator,
  MapController,
  MapView,
  OPERATION,
  OrbitController,
  OrbitView,
  OrbitViewport,
  OrthographicController,
  OrthographicView,
  OrthographicViewport,
  PointLight,
  PostProcessEffect,
  TRANSITION_EVENTS,
  TerrainController,
  Tesselator,
  TransitionInterpolator,
  UNIT,
  VERSION,
  View,
  Viewport,
  WebMercatorViewport,
  Widget,
  _CameraLight,
  _Component,
  _ComponentState,
  _GlobeController,
  _GlobeView,
  _GlobeViewport,
  _LayersPass,
  _PickLayersPass,
  _SunLight,
  _applyStyles,
  _compareProps,
  _count,
  _deepEqual,
  _fillArray,
  _flatten,
  _memoize,
  _mergeShaders,
  _registerLoggers,
  _removeStyles,
  assert,
  color,
  createIterable,
  fp64LowPart,
  getShaderAssembler,
  gouraudMaterial,
  log,
  phongMaterial,
  picking,
  project,
  project32,
  shadow
};
export default D;
